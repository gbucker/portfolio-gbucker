import linktree from "./linktree-schema";
import page from "./page-schema";
import post from "./post-schema";
import project from "./project-schema";

const schemas = [project, page, linktree, post];

export default schemas;
